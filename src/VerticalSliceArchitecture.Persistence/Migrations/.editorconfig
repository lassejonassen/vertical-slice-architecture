# EF Core generates and regenerates every file in this folder; hand-editing them to satisfy the
# repo's style/quality rules would just be undone by the next `dotnet ef migrations add`.
[*.cs]
generated_code = true
